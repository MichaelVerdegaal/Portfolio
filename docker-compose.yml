services:
  portfolio:
    build: .
    ports:
      - "8080:80"
    restart: unless-stopped

    # ─── Optional: bind-mount assets for live editing ──────────────────────────
    # Uncomment the volumes below to swap logo SVGs on the host without
    # rebuilding the image. Changes take effect on browser refresh.
    #
    # volumes:
    #   - ./assets/logos:/usr/share/nginx/html/assets/logos:ro
    #   - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
